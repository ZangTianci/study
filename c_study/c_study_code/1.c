// 输入三个数a,b,c；不管怎么输入，输出的时候，abc由大到小顺序输出，用函数封装实现

int sort(int *a, int *b, int *c){
    int *p;
    int 
    

}

int main(){
    int a, b, c;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);

    sort(&a,&b,&c);
}